""" ================================================================================ PROTOCOL_SINGULARITY_v2 — Automated Equilibrium Engine Constructed Version (Final) ================================================================================  This module implements a continuous-time simulation where each tick applies both a random adversarial attack and an automatic recovery event, modeling open systems under persistent environmental stress with passive homeostasis. Every line of code is preceded by its causal narrative, per Layer 2 Hybrid Sentence Syntax.  Resolved [COGNITION_GAP] assumptions:   1. max_ticks safety parameter prevents infinite loops in degenerate configs.   2. threat_range and recovery_rate are configurable with full validation.   3. tick_delay is purely cosmetic pacing.   4. Ticks are temporally independent Markov events.   5. Collapse triggers immediate loop termination; recovery is skipped. ================================================================================ """  import random import time from typing import List, Tuple, Union   # ============================================================================== # ADVERSARIAL SYSTEM WITH RECOVERY (Constructed Base — required substrate) # ==============================================================================  class AdversarialSystem:     """     Formal model of a resilient system under adversarial pressure with active     self-healing capabilities bounded by design ceiling and collapse floor.     """      # [SINGULARITY_HEADER]     # Intent  : Transform a stable system under adversarial force into a discrete     #           operational condition (BALANCED/STRAINED/COLLAPSED) while tracking     #           irreversible structural degradation; additionally, restore degraded     #           systems toward their design ceiling up to a rebalancing threshold.     # State   : System holds [Current_Stability, Max_Stability, Collapse_Threshold,     #           Strain_Ratio, Recovery_Balance_Threshold, State_Label]; expects     #           [Threat_Magnitude] or [Recovery_Rate].     # Action  : Validate input → check idempotent locks → compute transitions     #           (degradation or restoration) → enforce ceilings/floors → assess     #           state rebalance → return audit narrative.      def init(         self,         max_stability: float,         collapse_threshold: float,         strain_ratio: float = 0.5,         recovery_balance_threshold: float = 0.8,     ):         # [Entity: max_stability and collapse_threshold parameters]         # --(validation: enforce Assumption 3 — ceiling must exceed failure floor)--> [State: verified baseline or exception]         if max_stability <= collapse_threshold:             raise ValueError(                 "[COGNITION_GAP]: max_stability must exceed collapse_threshold; "                 "system cannot initialize in a pre-failed state."             )          # [Entity: collapse_threshold parameter]         # --(validation: enforce non-negative failure floor)--> [State: threshold verified]         if collapse_threshold < 0:             raise ValueError(                 "[COGNITION_GAP]: collapse_threshold must be non-negative."             )          # [Entity: strain_ratio parameter]         # --(validation: enforce meaningful strain detection bounds)--> [State: strain ratio verified in (0, 1]]         if not (0 < strain_ratio <= 1):             raise ValueError(                 "[COGNITION_GAP]: strain_ratio must be in (0, 1] for meaningful "                 "strain detection."             )          # [Entity: recovery_balance_threshold parameter]         # --(validation: enforce meaningful rebalancing bounds)--> [State: recovery threshold verified in (0, 1]]         if not (0 < recovery_balance_threshold <= 1):             raise ValueError(                 "[COGNITION_GAP]: recovery_balance_threshold must be in (0, 1] for "                 "meaningful state rebalancing."             )          # [Entity: validated max_stability]         # --(assignment to instance)--> [State: design ceiling established]         self.max_stability: float = float(max_stability)          # [Entity: validated max_stability]         # --(assignment to instance)--> [State: current stability initialized at full capacity]         self.stability: float = float(max_stability)          # [Entity: validated collapse_threshold]         # --(assignment to instance)--> [State: absolute failure floor established]         self.threshold: float = float(collapse_threshold)          # [Entity: validated strain_ratio]         # --(assignment to instance)--> [State: strain trigger threshold configured]         self.strain_ratio: float = float(strain_ratio)          # [Entity: validated recovery_balance_threshold]         # --(assignment to instance)--> [State: rebalancing trigger threshold configured]         self.recovery_balance_threshold: float = float(recovery_balance_threshold)          # [Entity: initialized stability and threshold]         # --(state label assignment)--> [State: system declared BALANCED at origin]         self.system_state: str = "BALANCED"      def inject_threat(self, threat_magnitude: Union[int, float]) -> str:         """         Process a single adversarial event and return an audit narrative.         """         # [Entity: incoming threat parameter]         # --(type validation: ensure numeric input)--> [State: type-verified]         if not isinstance(threat_magnitude, (int, float)):             raise TypeError(                 "[COGNITION_GAP]: threat_magnitude must be a real number."             )          # [Entity: incoming threat parameter]         # --(domain validation: enforce non-negative threat universe)--> [State: threat verified non-negative]         if threat_magnitude < 0:             raise ValueError(                 "[COGNITION_GAP]: Negative threat_magnitude is outside Input Universe "                 "(restorative forces are not handled by inject_threat)."             )          # [Entity: system state flag]         # --(idempotency check: terminal states reject new events)--> [State: collapse lock assessed]         if self.system_state == "COLLAPSED":             # [Entity: collapsed system]             # --(early return: prevent undefined operations on dead state)--> [State: caller informed of persistent failure]             return (                 f"[STATE LOCKED -> {self.system_state}]: System has already collapsed. "                 f"Threat of {threat_magnitude} encounters total structural failure."             )          # [Entity: current stability and validated threat]         # --(subtraction: measure remaining structural margin)--> [State: net_equilibrium computed]         net_equilibrium: float = self.stability - threat_magnitude          # [Entity: net_equilibrium and collapse_threshold]         # --(comparative evaluation: safety-margin breach check)--> [State: collapse condition assessed]         if net_equilibrium < self.threshold:             # [Entity: system's operational state]             # --(terminal transition: irreversible collapse per safety-margin model)--> [State: COLLAPSED]             self.system_state = "COLLAPSED"              # [Entity: remaining stability energy]             # --(total depletion: structural integrity annihilated)--> [State: stability zeroed]             self.stability = 0.0              # [Entity: collapse narrative]             # --(return to caller)--> [State: caller informed of terminal failure with causal reason]             return (                 f"💥 [CRITICAL SHIFT -> {self.system_state}]: "                 f"Threat ({threat_magnitude}) reduced equilibrium to {net_equilibrium:.1f}, "                 f"breaching threshold ({self.threshold}). Total structural failure!"             )          # [Entity: threat_magnitude and current stability]         # --(proportional comparison: assess if threat exceeds strain tolerance)--> [State: strain condition assessed]         elif threat_magnitude > (self.stability * self.strain_ratio):             # [Entity: system's operational state]             # --(degraded transition: partial damage, functionality preserved)--> [State: STRAINED]             self.system_state = "STRAINED"              # [Entity: threat_magnitude]             # --(proportional absorption: 30% of threat energy permanently scars structure)--> [State: stability degraded by shock absorption]             damage: float = threat_magnitude * 0.3             self.stability -= damage              # [Entity: strain narrative]             # --(return to caller)--> [State: caller informed of partial failure with remaining capacity]             return (                 f"⚠️  [STATE SHIFT -> {self.system_state}]: "                 f"Absorbed heavy shock. Stability degraded to {self.stability:.1f}."             )          # [Entity: weak threat relative to stability and strain ratio]         # --(neutralization: no structural cost)--> [State: BALANCED maintained]         else:             # [Entity: system's operational state]             # --(affirmation: no state change required)--> [State: BALANCED reaffirmed]             self.system_state = "BALANCED"              # [Entity: equilibrium narrative]             # --(return to caller)--> [State: caller informed of successful defense]             return (                 f"🛡️  [STATE MAINTAINED -> {self.system_state}]: "                 f"Threat ({threat_magnitude}) absorbed smoothly."             )      def auto_recovery_tick(self, recovery_rate: Union[int, float]) -> str:         """         Apply passive restorative energy to the system, enforcing the design         ceiling and triggering state rebalancing when sufficient recovery is         achieved. Identical logic to recover_stability but framed as automatic         homeostasis rather than active intervention.         """         # [Entity: incoming recovery_rate parameter]         # --(type validation: ensure numeric input)--> [State: type-verified]         if not isinstance(recovery_rate, (int, float)):             raise TypeError(                 "[COGNITION_GAP]: recovery_rate must be a real number."             )          # [Entity: incoming recovery_rate parameter]         # --(domain validation: enforce non-negative restorative universe)--> [State: recovery_rate verified non-negative]         if recovery_rate < 0:             raise ValueError(                 "[COGNITION_GAP]: Negative recovery_rate is outside Input Universe "                 "(damaging events are handled by inject_threat, not auto_recovery_tick)."             )          # [Entity: system state flag]         # --(collapse guard: enforce Rule A — collapsed systems cannot self-heal)--> [State: recovery eligibility assessed]         if self.system_state == "COLLAPSED":             # [Entity: collapsed system]             # --(early return: prevent impossible self-healing on dead state)--> [State: caller informed of persistent failure]             return "❌ [RECOVERY FAILED]: System is offline."          # [Entity: current stability and validated recovery_rate]         # --(ceiling enforcement: apply Rule B — min prevents exceeding max_stability)--> [State: new stability computed within bounds]         old_stability: float = self.stability         self.stability = min(self.max_stability, self.stability + recovery_rate)          # [Entity: old and new stability values]         # --(difference calculation: measure actual restoration achieved)--> [State: actual_restored computed]         actual_restored: float = self.stability - old_stability          # [Entity: current stability, max_stability, and recovery_balance_threshold]         # --(threshold comparison: assess if recovered capacity warrants state rebalancing)--> [State: transition condition evaluated]         if (             self.stability >= self.max_stability * self.recovery_balance_threshold             and self.system_state == "STRAINED"         ):             # [Entity: system's operational state]             # --(restorative transition: Rule C — sufficient recovery shifts STRAINED back to BALANCED)--> [State: BALANCED]             self.system_state = "BALANCED"              # [Entity: recovery narrative with state transition]             # --(return to caller)--> [State: caller informed of full recovery and rebalancing]             return (                 f"🟢 [STATE RECOVERED -> {self.system_state}]: "                 f"Restored +{actual_restored:.1f} stability ({self.stability:.1f}/{self.max_stability})."             )          # [Entity: recovery outcome without state transition]         # --(return to caller)--> [State: caller informed of partial or idempotent recovery]         return (             f"⚡ [AUTO-RECOVERY]: Restored +{actual_restored:.1f} stability -> "             f"Current: {self.stability:.1f}/{self.max_stability}."         )   # ============================================================================== # LAYER 3: GRAVITY CHECK (AdversarialSystem) # ============================================================================== # Origin — #     This engine exists because dynamic systems (biological, infrastructural, #     organizational) do not fail all at once, nor do they heal without bound. #     They degrade through repeated adversarial contact and regenerate toward a #     design ceiling between contacts. The safety-margin collapse model captures #     the reality that resilient systems require a minimum operational reserve; #     falling below that reserve triggers cascading failure. The recovery ceiling #     (min with max_stability) captures the physical law that no system can #     exceed its original design capacity without structural modification. # # Boundary — #     · If threat_magnitude or recovery_rate is negative or non-numeric, #       ValueError/TypeError halts execution immediately. #     · If the system is already COLLAPSED, both methods return idempotent #       lock/failure messages without mutating state further. #     · If max_stability <= collapse_threshold at initialization, ValueError #       prevents creation of an invalid object. #     · If strain_ratio or recovery_balance_threshold is outside (0, 1], #       ValueError prevents degenerate threshold logic. #     · If recovery_rate is zero or the system is already at max_stability, #       actual_restored becomes zero — idempotent "no change" narrative. # # Equilibrium — #     After inject_threat: BALANCED, STRAINED, or COLLAPSED. #     After auto_recovery_tick: COLLAPSED (blocked), STRAINED (partial), or #     BALANCED (rebalanced). No undefined intermediate states. # ==============================================================================   # ============================================================================== # AUTOMATED EQUILIBRIUM LOOP # ==============================================================================  class EquilibriumLoop:     """     Orchestrates a continuous while-loop where each tick applies a random     adversarial attack followed by an automatic recovery event, modeling open     systems under persistent environmental stress with passive homeostasis.     """      # [SINGULARITY_HEADER]     # Intent  : Model an open system under continuous environmental stress with     #           passive homeostatic repair, revealing the dynamic tug-of-war     #           between degradation and restoration over an unpredictable lifespan.     # State   : Loop holds [AdversarialSystem, threat_range, recovery_rate,     #           tick_delay, max_ticks, current_tick, audit_log]; expects     #           [execution trigger].     # Action  : Validate config → initialize system → enter while loop → generate     #           random threat → inject threat → append audit → check collapse →     #           apply auto-recovery → append audit → increment tick → delay →     #           check max_ticks → return complete forensic record.      def init(         self,         max_stability: float,         collapse_threshold: float,         threat_range: Tuple[float, float],         recovery_rate: float,         tick_delay: float = 0.5,         max_ticks: int = 1000,         strain_ratio: float = 0.5,         recovery_balance_threshold: float = 0.8,     ):         # [Entity: threat_range bounds]         # --(validation: enforce simulation Assumption 1 — well-ordered non-negative bounds)--> [State: threat bounds verified]         min_threat, max_threat = threat_range         if min_threat < 0 or max_threat < 0:             raise ValueError(                 "[COGNITION_GAP]: threat_range bounds must be non-negative."             )         if min_threat > max_threat:             raise ValueError(                 "[COGNITION_GAP]: threat_range must be well-ordered (min <= max)."             )          # [Entity: recovery_rate parameter]         # --(validation: enforce simulation Assumption 2 — non-negative restorative energy)--> [State: recovery rate verified]         if recovery_rate < 0:             raise ValueError(                 "[COGNITION_GAP]: recovery_rate must be non-negative."             )          # [Entity: max_ticks parameter]         # --(validation: enforce simulation Assumption 4 — at least one tick observed)--> [State: tick budget verified]         if max_ticks < 1:             raise ValueError(                 "[COGNITION_GAP]: max_ticks must be >= 1."             )          # [Entity: tick_delay parameter]         # --(validation: enforce non-negative latency)--> [State: delay verified cosmetic-only]         if tick_delay < 0:             tick_delay = 0.0          # [Entity: validated design parameters and thresholds]         # --(composition: instantiate formal adversarial substrate with recovery)--> [State: AdversarialSystem initialized]         self.system = AdversarialSystem(             max_stability=max_stability,             collapse_threshold=collapse_threshold,             strain_ratio=strain_ratio,             recovery_balance_threshold=recovery_balance_threshold,         )          # [Entity: validated threat bounds]         # --(assignment to instance)--> [State: stochastic sampling range configured]         self.threat_min: float = float(min_threat)         self.threat_max: float = float(max_threat)          # [Entity: validated recovery_rate]         # --(assignment to instance)--> [State: passive homeostatic energy configured]         self.recovery_rate: float = float(recovery_rate)          # [Entity: validated tick_delay]         # --(assignment to instance)--> [State: inter-tick latency configured]         self.tick_delay: float = float(tick_delay)          # [Entity: validated max_ticks]         # --(assignment to instance)--> [State: safety timeout configured]         self.max_ticks: int = max_ticks          # [Entity: tick counter initialized to zero]         # --(assignment to instance)--> [State: temporal index at origin]         self.current_tick: int = 0          # [Entity: empty list structure]         # --(initialization: prepare ordered forensic container)--> [State: audit_log ready for capture]         self.audit_log: List[str] = []      def run(self) -> Tuple[List[str], str, float, int]:         """         Execute the continuous equilibrium loop and return the complete audit         trail, final state, final stability, and the tick at which termination         occurred.         """         # [Entity: simulation parameters and empty audit_log]         # --(header generation: establish forensic trail origin)--> [State: audit_log initialized with session metadata]         self.audit_log.append(             "=========================================================="         )         self.audit_log.append(             f"AUTOMATED EQUILIBRIUM ENGINE | Max Stability: {self.system.max_stability:.1f} | "             f"Threshold: {self.system.threshold:.1f} | Recovery: {self.recovery_rate:.1f}/tick"         )         self.audit_log.append(             "=========================================================="         )          # [Entity: system state flag, max_ticks limit, and current_tick counter]         # --(loop guard: enforce termination when terminal state reached or safety budget exhausted)--> [State: while condition evaluated]         while (             self.system.system_state != "COLLAPSED"             and self.current_tick < self.max_ticks         ):             # [Entity: current tick counter]             # --(increment: advance temporal index for current iteration)--> [State: tick counter updated to next position]             self.current_tick += 1              # [Entity: tick identity]             # --(log entry creation: document temporal boundary for forensic trace)--> [State: tick header appended to audit_log]             self.audit_log.append(f"\n--- TIME TICK T+{self.current_tick} ---")              # [Entity: Python random module and threat bounds]             # --(stochastic sampling: model unpredictable adversarial intensity for current tick)--> [State: threat_force generated]             threat_force = round(random.uniform(self.threat_min, self.threat_max), 1)              # [Entity: threat identity and magnitude]             # --(log entry creation: document incoming attack vector)--> [State: threat event logged]             self.audit_log.append(                 f"[EVENT 1: THREAT]: Attack magnitude = {threat_force}"             )              # [Entity: AdversarialSystem instance and generated threat]             # --(state transition execution: process adversarial event per formal protocol)--> [State: attack result produced and system state updated]             threat_result = self.system.inject_threat(threat_force)              # [Entity: threat result narrative]             # --(audit capture: preserve causal outcome in ordered record)--> [State: threat result appended to audit_log]             self.audit_log.append(f"   └── {threat_result}")              # [Entity: system state flag post-threat]             # --(emergency evaluation: detect if threat triggered collapse to bypass recovery)--> [State: termination condition assessed]             if self.system.system_state == "COLLAPSED":                 # [Entity: collapsed system]                 # --(loop break: enforce immediate termination since dead systems cannot process recovery)--> [State: loop exited, no recovery executed]                 break              # [Entity: AdversarialSystem instance and configured recovery_rate]             # --(restoration execution: process automatic homeostatic repair per formal protocol)--> [State: recovery result produced and system state updated]             recovery_result = self.system.auto_recovery_tick(self.recovery_rate)              # [Entity: recovery event label and result narrative]             # --(audit capture: preserve causal outcome in ordered record)--> [State: recovery event logged]             self.audit_log.append("[EVENT 2: HEALING]: Auto-recovery tick active")             self.audit_log.append(f"   └── {recovery_result}")              # [Entity: simulation clock and tick_delay parameter]             # --(temporal suspension: simulate inter-tick latency for realistic pacing)--> [State: execution paused, preserving causal ordering between ticks]             if self.tick_delay > 0:                 time.sleep(self.tick_delay)              # [Entity: current_tick and max_ticks parameters]             # --(safety evaluation: detect if tick budget exhausted without collapse)--> [State: timeout condition assessed]             if self.current_tick >= self.max_ticks:                 # [Entity: max_ticks limit]                 # --(timeout break: prevent infinite loop in degenerate configurations)--> [State: loop exited with survival outcome]                 self.audit_log.append(                     f"\n[TIMEOUT]: Reached max_ticks ({self.max_ticks}) without collapse."                 )                 break          # [Entity: completed audit_log and final system metrics]         # --(footer generation: summarize terminal condition for caller analysis)--> [State: audit_log finalized with session conclusion]         self.audit_log.append(             "\n=========================================================="         )         self.audit_log.append(             f"💀 SIMULATION TERMINATED AT TICK T+{self.current_tick}"         )         self.audit_log.append(             f"Final System State: {self.system.system_state} | "             f"Final Stability: {self.system.stability:.1f}/{self.system.max_stability:.1f}"         )         self.audit_log.append(             "=========================================================="         )          # [Entity: finalized audit_log, terminal state, remaining stability, and termination tick]         # --(return to caller: deliver complete forensic record and terminal metrics)--> [State: caller receives immutable simulation outcome]         return (             self.audit_log,             self.system.system_state,             self.system.stability,             self.current_tick,         )   # ============================================================================== # LAYER 3: GRAVITY CHECK (EquilibriumLoop) # ============================================================================== # Origin — #     This loop exists because real systems do not experience isolated attacks #     followed by planned recovery phases. They exist in open environments where #     stress and repair occur simultaneously and continuously. The while-loop #     form captures the dynamic equilibrium of sustained conflict: each tick #     is a microcosm of the full adversarial-recovery cycle, and the net #     trajectory over many ticks reveals whether the system's homeostasis is #     sufficient to survive its environment or whether cumulative damage #     eventually overwhelms repair capacity. The random threat generation #     models environmental unpredictability; the fixed recovery rate models #     passive structural resilience (e.g., biological healing, automatic #     failover, thermal dissipation). # # Boundary — #     · If threat_range is inverted or negative, ValueError prevents impossible #       stochastic generation. #     · If recovery_rate is negative, ValueError prevents damaging "recovery". #     · If max_ticks < 1, ValueError prevents vacuous execution. #     · If the system collapses during a threat phase, the loop breaks #       immediately — recovery is bypassed, no post-collapse processing. #     · If max_ticks is reached without collapse, the loop terminates with a #       timeout message — the system survived the campaign. #     · If tick_delay is negative, it is clamped to zero (non-blocking). #     · If the underlying AdversarialSystem is initialized with invalid params, #       the constructor raises ValueError at birth, not under load. # # Equilibrium — #     After run(), the caller receives: #       1. A complete ordered audit log of every tick. #       2. The final system_state string (BALANCED, STRAINED, or COLLAPSED). #       3. The final stability float. #       4. The termination tick integer. #     The loop operates on its own AdversarialSystem instance; it does not #     mutate external state. The log is append-only and ordered by tick index, #     providing a forensic trail. In collapse, the log reveals the exact tick #     at which failure occurred. In survival (timeout), the log shows the full #     max_ticks trajectory. There are no ambiguous outcomes — the terminal #     condition is always either collapse or survival timeout. # ==============================================================================   # ============================================================================== # SIMULATION DEMONSTRATION # ============================================================================== if name == "main":     # [Entity: design requirements]     # --(instantiation: create loop with 100.0 max stability, 10.0 threshold, [15,95] threat range, 12.0 recovery)--> [State: loop configured and ready]     loop = EquilibriumLoop(         max_stability=100.0,         collapse_threshold=10.0,         threat_range=(15.0, 95.0),         recovery_rate=12.0,         tick_delay=0.5,         max_ticks=1000,         strain_ratio=0.5,         recovery_balance_threshold=0.8,     )      # [Entity: EquilibriumLoop instance]     # --(execution: run continuous campaign and capture forensic output)--> [State: simulation completed, audit trail returned]     audit_log, final_state, final_stability, termination_tick = loop.run()      # [Entity: audit_log entries]     # --(display: render ordered forensic record to stdout for human analysis)--> [State: simulation results visible to operator]     for line in audit_log:         print(line)      # [Entity: final metrics]     # --(summary output: present terminal condition in isolated line for quick scanning)--> [State: operator informed of session conclusion]     print(f"\n>>> OPERATOR SUMMARY: State={final_state} | Stability={final_stability:.1f}/{loop.system.max_stability:.1f} | Ticks={termination_tick}")